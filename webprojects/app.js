

function update() {
    var bulb = document.getElementById("bulb")
    var like = document.getElementById("like")
    bulb.classList.toggle("toggle")
    // var likeprev = like.innerText
    // likec = parseInt(likeprev)
    // like.innerText = likec + 1
}
function goBack() {
    window.history.back();
}